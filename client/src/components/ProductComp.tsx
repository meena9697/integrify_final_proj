import React from 'react'
import { Products } from '../redux/types';

export default function ProductComp({
    seleted_brand,
  }: {
    seleted_brand: Products;
  }){
return(
    <h1> adgjol</h1>
)
  }